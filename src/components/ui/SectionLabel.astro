---
interface Props {
  index: string;
  text: string;
}

const { index, text } = Astro.props;
---

<div class="flex items-center gap-4">
  <span class="font-mono text-xs uppercase tracking-[0.2em] text-steel-500">{index}/ {text}</span>
  <span class="h-px flex-1 bg-line" aria-hidden="true"></span>
</div>
